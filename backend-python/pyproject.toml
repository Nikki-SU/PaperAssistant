[project]
name = "paperassistant-backend"
version = "0.1.0"
description = "PaperAssistant FastAPI sidecar backend"
requires-python = ">=3.11"
license = { text = "AGPL-3.0-only" }

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
